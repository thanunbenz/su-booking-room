# Server Configuration
APP_NAME=su-booking-room
APP_ENV=development
APP_PORT=8080
APP_HOST=localhost

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=booking_room_db
DB_SSL_MODE=disable

# JWT Configuration
JWT_SECRET=your-secret-key-here
JWT_EXPIRE=24h

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Redis Configuration (Optional)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Email Configuration (Optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
